export default function Comments() {
  return (
    <div className="dark:bg-primary-bg bg-secondary-bg border dark:border-zinc-800 border-zinc-200 rounded-lg p-6 dark:text-zinc-400 text-zinc-600">
      Comments are disabled for now while we simplify the template. We can add a
      different comment system later if you want one.
    </div>
  );
}
